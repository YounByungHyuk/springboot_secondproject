spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true
  datasource: #db정보 추가
    url: jdbc:h2:mem:testdb
    username: sa
  h2: #콘솔활성화
    console:
      enabled: true
  jwt:
    issuer : ybh0311@naver.com
    secret_key :  study-spring